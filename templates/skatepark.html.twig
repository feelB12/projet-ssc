{% extends "base.html.twig" %}

{% block metatitle %}
    {{ skatepark.title }} | Skateboard Social Club
{% endblock metatitle %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/details.css') }}">
    <link rel="stylesheet" href="{{ asset('css/burger.css') }}">
{% endblock stylesheets %}

{% block main %}
    <div class="card">
        <img class="title" src="{{ asset('icon/skateparks_titre.png') }}" alt="Skatepark">
        <img class="title" src="{{ asset('icon/skateparks.png') }}" alt="Skatepark">
        <div class="card-text">
            <h1>{{ skatepark.title }}</h1><br>
            <h3>{{ skatepark.description }}</h3><br>
            <h3>{{ skatepark.address }}</h3><br>
            <h3>{{ skatepark.town }}</h3><br>
            <h3>{{ skatepark.area }}</h3><br>

            {#  {% for club in skatepark.clubs %} #}
            {#    <p>{{ club.title }}</p> #}
            {#    {% endfor %} #}
        </div>
    </div>
{% endblock main %}