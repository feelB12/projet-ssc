{%  include "_header.html.twig" %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/details.css') }}">
    <link rel="stylesheet" href="{{ asset('css/burger.css') }}">
{% endblock stylesheets %}

{% block metatitle %}Hello{% endblock metatitle %}

{% block main %}
   <main>
       <section class="section">
           {% for sessions in lastSessions %}
           <div class="card">
               <div class="card-text">
                   <h2>{{ sessions.title }}</h2>
                   <h3>{{ sessions.description }}</h3>
               </div>
               <div class="card-image">
                   <!-- <img src="{{ session.image }}" alt="image de la session"> -->
               </div>
           </div>
          {% endfor %}
       </section>

       <section class="section">
           {% for skatepark in lastSkateparks %}
               <div class="card">
                   <div class="card-text">
                       <h2>{{ skatepark.title }}</h2>
                       <!--  if session.finished  <h3>La session est finie depuis :  session.finished | datetime(D-m-Y) }}</h3> -->
                   </div>
                   <div class="card-image">
                       <!-- <img src="{{ session.image }}" alt="image de la session"> -->
                   </div>
               </div>
           {% endfor %}
       </section>
       <section class="section">
           {% for shop in lastShops %}
               <div class="card">
                   <div class="card-text">
                       <h2>{{ shop.title }}</h2>
                       <!--  if session.finished  <h3>La session est finie depuis :  session.finished | datetime(D-m-Y) }}</h3> -->
                   </div>
                   <div class="card-image">
                       <!-- <img src="{{ shop.image }}" alt="image de la session"> -->
                   </div>
               </div>
           {% endfor %}
       </section>
       <section class="section">
           {% for club in lastClubs %}
               <div class="card">
                   <div class="card-text">
                       <h2>{{ club.title }}</h2>
                       <!--  if session.finished  <h3>La session est finie depuis :  session.finished | datetime(D-m-Y) }}</h3> -->
                   </div>
                   <div class="card-image">
                       <!-- <img src="{{ club.image }}" alt="image de la session"> -->
                   </div>
               </div>
           {% endfor %}
       </section>

       <!--
        <article class="top">
            <img class="title left logo resp top" src="{{ asset('icon/sessions_titre.png') }}" alt="session">
            <img class="title right logo resp top" src="{{ asset('icon/session.png') }}" alt="session-icon">
        </article>
        <article class="top">
            <img class="title" src="{{ asset('icon/skateparks_titre.png') }}" alt="Skatepark">
            <img class="title" src="{{ asset('icon/skateparks.png') }}" alt="Skatepark">
        </article>
        <article class="top">
            <img class="title left logo resp top" src="{{ asset('icon/clubs_titre.png') }}" alt="Club">
            <img class="title right logo resp top" src="{{ asset('icon/club.png') }}" alt="Club-icon">
        </article>
        <article class="top">
            <img class="title" src="{{ asset('icon/shops_titre.png') }}" alt="Shop">
            <img class="title" src="{{ asset('icon/shops.png') }}" alt="Shop">
        </article>
         -->
    </main>

{% endblock main %}

{%  include "_footer.html.twig" %}


